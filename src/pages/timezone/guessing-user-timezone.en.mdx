import { Callout } from "nextra/components";

# Guessing user zone

Return the user's time zone.

<Callout>
  This requires the [Timezone](../plugin/timezone) plugin to work
</Callout>

```javascript
dayjs.extend(utc);
dayjs.extend(timezone);
dayjs.tz.guess(); // America/Chicago
```
