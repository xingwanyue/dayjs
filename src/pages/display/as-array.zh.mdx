import { Callout } from "nextra/components";

# As Array

Returns an array that mirrors the parameters

<Callout>
  This requires the [ToArray](../plugin/to-array) plugin to work
</Callout>

```js
dayjs.extend(toArray);

dayjs("2019-01-25").toArray(); // [ 2019, 0, 25, 0, 0, 0, 0 ]
```
